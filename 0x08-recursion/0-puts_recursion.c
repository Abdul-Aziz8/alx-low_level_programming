#include "main.h"
#include <stdio.h>

/**
 * prints new string folloed by a new line
 *@*s: is a pointer to a char
 *
 * return: a string
 */
void _puts_recursion(char *s)
{
	char s[20] = "Hello";

	printf("%s\n", s);
	return (0);
}
